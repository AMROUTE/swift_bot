# Citation answer before LLM answer

Swift Bot's MVP returns引用回答 instead of calling an LLM to generate free-form answers. We chose this because the first product risk is trust and auditability, not fluency; every answer must be grounded in片段 and show引用 before the project adds model-generated prose.
